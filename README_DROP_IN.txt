Drop-in fixes for the build errors.

1. Copy `lib/supabaseServer.ts` into your project's `lib/supabaseServer.ts`.
2. Copy `lib/types.ts` into your project's `lib/types.ts`.
3. Commit and push to GitHub.
4. Let Render rebuild.

These files include backward-compatible exports:
- `getServiceClient`
- `ownerEmails`
- `OrderRecord`
